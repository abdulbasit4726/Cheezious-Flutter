import 'package:flutter/material.dart';

class PromosScreen extends StatelessWidget {
  static const routeName = '/PromosScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Promos Screen')));
  }
}
