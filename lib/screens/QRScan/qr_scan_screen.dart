import 'package:flutter/material.dart';

class QRScanScreen extends StatelessWidget {
  static const routeName = '/QRScanScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('QRScan Screen')));
  }
}
